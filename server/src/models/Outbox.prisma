model Outbox {
  id        String   @id @default(cuid())
  to        String
  subject   String
  html      String
  retries   Int
  lastRetry DateTime
  error     String
  createdAt DateTime @default(now())
}
