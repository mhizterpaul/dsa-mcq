model Outbox {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  to        String
  subject   String
  html      String
  retries   Int
  lastRetry DateTime
  error     String
  createdAt DateTime @default(now())
}
