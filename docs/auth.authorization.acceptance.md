# Documentation: auth.authorization.acceptance.test.ts

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: me rejects unauthenticated access (no token)

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: sync rejects unauthenticated access (no token)

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: questions rejects unauthenticated access (no token)

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: devops rejects unauthenticated access (no token)

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: action rejects unauthenticated access (no token)

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: me rejects malformed authorization header

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: sync rejects malformed authorization header

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: questions rejects malformed authorization header

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: devops rejects malformed authorization header

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: action rejects malformed authorization header

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: me allows lowercase bearer prefix

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: sync allows lowercase bearer prefix

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: questions allows lowercase bearer prefix

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: devops allows lowercase bearer prefix

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: action allows lowercase bearer prefix

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: me handles database failure gracefully (returns sanitized 500)

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: sync handles database failure gracefully (returns sanitized 500)

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: questions handles database failure gracefully (returns sanitized 500)

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: devops handles database failure gracefully (returns sanitized 500)

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: action handles database failure gracefully (returns sanitized 500)

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: devops endpoint enforces admin role (using DB role)

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: devops endpoint prevents privilege escalation via tampered JWT role

- Status: ✅ passed

## Feature: Cross-Endpoint Authorization Enforcement

### Scenario: devops endpoint allows admin access with various role casings

- Status: ✅ passed
